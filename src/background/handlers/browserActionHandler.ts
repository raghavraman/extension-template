import { BrowserActionHandler } from 'src/shared/messages/BrowserActionMessages';

export const browserActionHandler = new BrowserActionHandler({
    disableBrowserAction(context) {},
    enableBrowserAction(context) {},
});
